# ⚠️ LEGACY — Nicht mehr verwenden!

Diese Migrations sind versions-basiert (über plugin.json) und werden nur
bei expliziten Version-Upgrades via GitHub-Release ausgeführt.

## Neue Updates → `../updates/` Ordner

Dateien in `../updates/` werden automatisch beim Plugin-Start via
PluginUpdater ausgeführt. Naming: `001_beschreibung.js` oder `001_beschreibung.sql`
