/**
 * Greeting - Platzhalter fuer seitenweite Ergaenzungen.
 *
 * Die eigentliche Formularlogik steckt weiterhin im View-Partial
 * `greeting-scripts.ejs`, weil sie eng an den gerenderten Feldern haengt.
 * Diese Datei existiert, damit die Seiten ein angemeldetes Skript einreihen
 * koennen, ohne dass jede View einen eigenen Inline-Block braucht.
 */
(function () {
    'use strict';
    // Noch nichts Seitenweites noetig.
})();
