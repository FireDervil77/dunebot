<%- include('partials/greeting-kopf', {
  guildId, tr,
  icon: 'fa-solid fa-rocket',
  titel: tr('greeting:NAV.BOOST'),
  beschreibung: tr('greeting:BOOST.SUBTITLE')
}) %>

<div class="row">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><i class="fa-solid fa-rocket me-2"></i><%= tr('greeting:TAB_BOOST') %></h3>
      </div>
      <div class="card-body">
        <%- include('greeting-content', { type: 'boost', channels, roles, settings, guildId }) %>
      </div>
    </div>
  </div>

  <%- include('partials/greeting-vorschau') %>
</div>

<%- includePluginPartial('greeting', 'greeting-scripts', locals) %>
